"use client";
import type { NavUniverse } from "@/lib/navigation";
import type { SafeUser } from "@/lib/auth";
export function CineMobileMenu({ open, onClose, onSearch, universes, user, onOpenCart }: { open: boolean; onClose: () => void; onSearch: () => void; universes: NavUniverse[]; user: SafeUser | null; onOpenCart: () => void }) {
  return null;
}
