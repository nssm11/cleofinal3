"use client";
export function SearchSurface({ open, onClose }: { open: boolean; onClose: () => void }) {
  // Functionality moved into SiteHeader for Swiss minimal — this is a stub to keep imports valid
  return null;
}
